# 开发规则

1. 明确用户需求，在不知道或不确定的，可以向用户提问
2. 在开发渲染进程代码中，采用组件化开发，不要在一个文件内写太多代码，拆分组件实现功能
3. 在渲染进程代码中, 请遵循 `prmevue-theme-colors-skill` 技能来使用主题颜色以及定义主题颜色，**禁止直接**使用tailwindcss颜色变量, 布局则采用tailwindcss的布局方案

## 开发规范

### app-admin

### app-server
