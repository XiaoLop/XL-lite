# 开发规则

1. 明确用户需求，在不知道或不确定的，可以向用户提问

## 开发规范

### app-admin

> 前端代码

1. 在开发中，采用组件化开发，不要在一个文件内写太多代码，拆分组件实现功能
2. 请遵循 `prmevue-theme-colors-skill` 技能来使用主题颜色以及定义主题颜色，**禁止直接**使用tailwindcss颜色变量, 布局则采用tailwindcss的布局方案

### app-server

> 后端代码
